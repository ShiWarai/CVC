# Публикация образов в GHCR после успешного ML Pipeline:
# - main → ghcr.io/<owner>/cvc-robot-dog:main
# - dev  → ghcr.io/<owner>/cvc-robot-dog:dev (staging runtime)

name: Publish Docker image

on:
  workflow_run:
    workflows: ["ML Pipeline - Train and Publish"]
    types: [completed]
    branches: [main, dev]

permissions:
  contents: read
  packages: write

jobs:
  publish-prod:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main'
    name: Build and push prod to GHCR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image name
        run: echo "IMAGE_BASE=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/cvc-robot-dog" >> $GITHUB_ENV

      - name: Build and push runtime (prod)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.IMAGE_BASE }}:main
            ${{ env.IMAGE_BASE }}:${{ github.event.workflow_run.head_sha }}
          push: true
          provenance: false

  publish-dev:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'dev'
    name: Build and push dev runtime to GHCR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image name
        run: echo "IMAGE_BASE=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/cvc-robot-dog" >> $GITHUB_ENV

      - name: Build and push runtime (dev tag)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.IMAGE_BASE }}:dev
            ${{ env.IMAGE_BASE }}:${{ github.event.workflow_run.head_sha }}
          push: true
          provenance: false

  notify-telegram-on-publish-prod:
    name: Notify Telegram on prod image published
    if: always() && needs.publish-prod.result == 'success'
    needs: [publish-prod]
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram notification (silent)
        uses: appleboy/telegram-action@v1.0.1
        continue-on-error: true
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          disable_notification: true
          message: |
            *Образ CVC (prod) опубликован в GHCR*
            Репозиторий: `${{ github.repository }}`
            Ветка: `${{ github.event.workflow_run.head_branch }}`
            Образ: `ghcr.io/${{ github.repository_owner }}/cvc-robot-dog:main`
            [Открыть run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})

  notify-telegram-on-publish-dev:
    name: Notify Telegram on dev image published
    if: always() && needs.publish-dev.result == 'success'
    needs: [publish-dev]
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram notification (silent)
        uses: appleboy/telegram-action@v1.0.1
        continue-on-error: true
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          disable_notification: true
          message: |
            *Образ CVC (dev) опубликован в GHCR*
            Репозиторий: `${{ github.repository }}`
            Ветка: `dev`
            Образ: `ghcr.io/${{ github.repository_owner }}/cvc-robot-dog:dev`
            [Открыть run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
