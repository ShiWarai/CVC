version: '3.8'

# Docker Compose файл для CUDA версии
# Использование: docker-compose -f docker-compose.cuda.yml up --build

services:
  cvc-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PYTORCH_VERSION: cuda
    container_name: cvc-api
    # Для CUDA требуется runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    ports:
      - "8000:8000"
    volumes:
      # Монтируем модели, чтобы они сохранялись между перезапусками
      - ./models:/app/models
      - ./checkpoints:/app/checkpoints
      # Монтируем кэш Hugging Face, чтобы не скачивать оригинальную модель каждый раз
      - ./cache/huggingface:/app/.cache/huggingface
      # Монтируем директорию для базы данных (монтирование файла может создать директорию вместо файла)
      - ./db:/app/db
      # Монтируем конфигурацию (опционально, для изменения без пересборки)
      - ./config.yaml:/app/config.yaml:ro
      # Монтируем данные (опционально)
      - ./data:/app/data:ro
    environment:
      # Переменные окружения для Hugging Face
      - HF_HOME=/app/.cache/huggingface
      # Токен для доступа к gated моделям (загружается из .env файла)
      - HF_TOKEN=${HF_TOKEN}
      # Альтернативное имя переменной (на случай если используется другое)
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN}
      # Переменная для CUDA
      - CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES:-0}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/health', timeout=5).raise_for_status()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
